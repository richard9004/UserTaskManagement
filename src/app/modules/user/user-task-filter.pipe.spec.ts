import { UserTaskFilterPipe } from './user-task-filter.pipe';

describe('UserTaskFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserTaskFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
